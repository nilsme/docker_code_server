# README

Using `code-server` with docker build from Fedora for local use.

## Build images

```Shell script
docker-compose build
```

## Start code-server

```Shell script
docker-compose up -d
```

## Stop code-server

```Shell script
docker-compose down
```
